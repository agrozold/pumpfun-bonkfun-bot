# compute_units:
#   account_data_size: 512000  # Commented out - causes "max loaded accounts data size cap" error
enabled: true
env_file: .env
filters:
  bro_address: null
  listener_type: pumpportal
  match_string: null
  max_token_age: 15
name: bot-sniper-pump
platform: pump_fun
priority_fees:
  enable_dynamic: false
  enable_fixed: true
  fixed_amount: 300000
private_key: ${SOLANA_PRIVATE_KEY}
pumpportal:
  url: wss://pumpportal.fun/api/data
rpc_endpoint: ${SOLANA_NODE_RPC_ENDPOINT}
separate_process: true
trade:
  buy_amount: 0.01
  buy_slippage: 0.3
  exit_strategy: tp_sl
  max_hold_time: 86400  # 24 hours
  moon_bag_percentage: 20
  price_check_interval: 2
  sell_slippage: 0.3
  stop_loss_percentage: 0.7
  take_profit_percentage: 1.0
wss_endpoint: ${SOLANA_NODE_WSS_ENDPOINT}

# Pattern Detection (optional - disabled by default)
# Uncomment to enable pump pattern detection before buying
# pattern_detection:
#   enabled: true
#   volume_spike_threshold: 3.0    # 3x volume increase triggers signal
#   holder_growth_threshold: 0.5   # 50% holder growth triggers signal
#   min_whale_buys: 2              # Min whale buys in 30s for cluster signal
#   min_patterns_to_buy: 2         # Min patterns needed to trigger buy
#   pattern_only_mode: false       # If true, only buy when patterns detected

# Token Scoring (optional - filters tokens by Dexscreener metrics)
# scoring:
#   enabled: true
#   min_score: 70                  # Minimum score to buy (0-100)
#   volume_weight: 30              # Weight for volume spike score
#   buy_pressure_weight: 30        # Weight for buy/sell ratio score
#   momentum_weight: 25            # Weight for price momentum score
#   liquidity_weight: 15           # Weight for liquidity score

# Whale Copy Trading (optional - copy trades from smart money wallets)
# whale_copy:
#   enabled: true
#   wallets_file: "smart_money_wallets.json"
#   min_buy_amount: 0.5            # Only copy buys > 0.5 SOL
#   helius_api_key: "${HELIUS_API_KEY}"  # Optional, for faster tracking
